---
interface Props {
	pathname: string;
	postSlug?: string;
}

const isDev = import.meta.env.DEV;
---

{
	isDev && (
		<div class="fixed bottom-5 right-5 z-[99999]">
			<a
				href="/hualeide-studio-hidden/"
				class="rounded-full bg-sky-600 px-4 py-2 text-sm font-medium text-white shadow-lg hover:bg-sky-700"
				target="_blank"
				rel="noopener"
			>
				内容管理台
			</a>
		</div>
	)
}
